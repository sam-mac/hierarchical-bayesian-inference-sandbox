# Bayes-Tools

Sandbox for learning hierarchical Bayesian inference.

## Setup

Run `uv venv --python 3.13` then `source .venv/Scripts/activate` then `uv sync --extra dev` in development mode (e.g., with pytest), or `uv sync`.
